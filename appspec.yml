version: 0.0
os: linux
files:
  - source: confs/nginx/vhost.conf
    destination: /etc/nginx/conf.d/vhost.conf
  - source: confs/nginx/nginx.repo
    destination: /etc/yum.repos.d/nginx.repo
  - source: confs/php-fpm/www.conf
    destination: /etc/php-fpm.d/www.conf
  - source: scripts
    destination: /usr/local/bin
  - source: /
    destination: /var/www/app-v1
permissions:
  - object: /var/www/app-v1
    owner: www-data
    group: www-data
    mode: 755
    type:
      - directory
hooks:
  ApplicationStop:
    - location: /usr/local/bin/deregister_from_elb.sh
    - location: /usr/local/bin/stop_server.sh
  ApplicationStart:
    - location: /usr/local/bin/start_server.sh
    - location: /usr/local/bin/register_with_elb.sh
